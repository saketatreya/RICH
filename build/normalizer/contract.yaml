id: normalizer
description: Convert a string to uppercase
interface:
  operations:
  - name: run
    inputs:
      text: string
    outputs:
      result: string
    errors: []
dependencies: []
behavior:
- id: basic
  prose: Convert a string to uppercase
