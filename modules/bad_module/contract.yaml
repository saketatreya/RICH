name: bad_module
# DELIBERATELY MALFORMED — for M1 acceptance testing

interface:
  operations:
    - name: do_thing
      inputs:
        x: foobar_type          # not in v1 vocabulary
      outputs:
        y: string
    - name: do_thing            # duplicate name
      inputs: {}
      outputs: {}

dependencies:
  - nonexistent                 # doesn't reference an existing module

behavior:
  - id: ""                      # empty id
  - prose: "missing id field"
  - id: token_on_success        # duplicate id
    prose: "dup"